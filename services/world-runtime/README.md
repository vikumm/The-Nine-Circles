# World Runtime Bootstrap

This folder contains the VS-001 World Runtime placeholder.

Scope for VS-001:

- compile as a bootstrap executable;
- expose only project metadata;
- do not implement maps, movement, AI, combat, rewards or checkpoints.

Future work:

- VS-009 and VS-010 introduce authoritative map and movement after earlier gates pass.
